# shell out the config
terraform output config_map_aws_auth >> config-map-aws-auth.yaml

# apply the config via kubectl
kubectl apply -f config-map-aws-auth.yaml

# verify nodes have joined
kubectl get nodes --watch
